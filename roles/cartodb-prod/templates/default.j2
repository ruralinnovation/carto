server {
    listen 80;
    server_name {{ public_host }};

    # Tell Nginx and Passenger where your app's 'public' directory is
    root /home/ubuntu/cartodb/public;

    # Turn on Passenger
    passenger_enabled on;
    passenger_ruby /usr/bin/ruby;
}

server {
    listen 443;
    server_name {{ public_host }};

    #real_ip_header proxy_protocol;
    #real_ip_header X-Forwarded-For;
    #proxy_set_header X-Forwarded-Port $proxy_protocol_port;

    # Tell Nginx and Passenger where your app's 'public' directory is
    root /home/ubuntu/cartodb/public;

    # Turn on Passenger
    passenger_enabled on;
    passenger_ruby /usr/bin/ruby;

    ssl on;
    ssl_certificate /home/ubuntu/ssl/certificate.pem;
    ssl_certificate_key /home/ubuntu/ssl/key.pem;
}

server {
        listen       9090;
        server_name {{ public_host }};

        ssl on;
        ssl_certificate /home/ubuntu/ssl/certificate.pem;
        ssl_certificate_key /home/ubuntu/ssl/key.pem;

        # important, allows proxy connection to use http
        proxy_ssl_session_reuse off;

        location / {
            proxy_pass         http://{{ public_host }}:8080;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       10m;
            client_body_buffer_size    128k;

            proxy_connect_timeout      90;
            proxy_send_timeout         90;
            proxy_read_timeout         90;

            proxy_buffer_size          4k;
            proxy_buffers              4 32k;
            proxy_busy_buffers_size    64k;
            proxy_temp_file_write_size 64k;
        }

}

server {
        listen       9191;
        server_name {{ public_host }};

        ssl on;
        ssl_certificate /home/ubuntu/ssl/certificate.pem;
        ssl_certificate_key /home/ubuntu/ssl/key.pem;

        proxy_ssl_session_reuse off;

        location / {
            proxy_pass         http://{{ public_host }}:8181;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       10m;
            client_body_buffer_size    128k;

            proxy_connect_timeout      90;
            proxy_send_timeout         90;
            proxy_read_timeout         90;

            proxy_buffer_size          4k;
            proxy_buffers              4 32k;
            proxy_busy_buffers_size    64k;
            proxy_temp_file_write_size 64k;
        }

}